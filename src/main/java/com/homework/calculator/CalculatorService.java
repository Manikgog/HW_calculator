package com.homework.calculator;

public interface CalculatorService {
    String hello();
    Integer plus(Integer number1, Integer number2);
    Integer minus(Integer number1, Integer number2);
    Integer multiply(Integer number1, Integer number2);
    Integer divide(Integer number1, Integer number2);
}
